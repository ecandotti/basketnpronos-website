<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3 py-2">
  <a class="navbar-brand" href="{{ path('home') }}">Bissonico</a>
  <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="navbar-collapse collapse" id="navbarColor01" style="">
    <ul class="navbar-nav mr-auto">
        <li class="nav-item">
            <a class="nav-link" href="{{ path('home') }}">Accueil</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('notice') }}">Avis</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('join_us') }}">Rejoindre le VIP</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('gallery') }}">Gallerie</a>
        </li>
    </ul>
    {% if app.user %}
    <li class="nav-item dropdown list-unstyled">
        <a class="nav-link dropdown-toggle text-white" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
            Bonjour, <b>{{ app.user.pseudo}}</b>
        </a>
        <div class="dropdown-menu text-center">

        {% if app.user and is_granted('ROLE_ADMIN') %}
            <a class="dropdown-item" href="{{ path('admin_dashboard') }}">Mon compte</a>
        {% else %}
          <a class="dropdown-item" href="{{ path('user_dashboard') }}">Mon compte</a>
        {% endif %}

        <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="{{ path('app_logout') }}">Deconnexion</a>
        </div>
    </li>
    {% else %}
        <div>
            <a href='{{ path('app_login') }}' class="btn btn-primary">Connexion</a>
            <a href='{{ path('app_register') }}' class="btn btn-secondary ml-2">Nous rejoindre</a>
        </div>
    {% endif %}
  </div>
</nav>