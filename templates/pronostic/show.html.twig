{% extends 'base.html.twig' %}

{% block title %}Pronostic{% endblock %}

{% block body %}
    <h1>Pronostic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pronostic.id }}</td>
            </tr>
            <tr>
                <th>TeamA</th>
                <td>{{ pronostic.teamA }}</td>
            </tr>
            <tr>
                <th>TeamB</th>
                <td>{{ pronostic.teamB }}</td>
            </tr>
            <tr>
                <th>Winner</th>
                <td>{{ pronostic.winner }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ pronostic.createAt ? pronostic.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pronostic_index') }}">back to list</a>

    <a href="{{ path('pronostic_edit', {'id': pronostic.id}) }}">edit</a>

    {{ include('pronostic/_delete_form.html.twig') }}
{% endblock %}
