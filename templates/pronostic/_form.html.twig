{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.category) }}
    {{ form_row(form.content) }}
    <div class="my-2">
        <small class="text-muted">ND = Non définie</small><br>
        <small class="text-muted">G = Gagné</small><br>
        <small class="text-muted">P = Perdu</small>
        {{ form_row(form.result) }}
    </div>
    <div class="d-flex my-4">
    {{ form_row(form.valider) }}
    <a href="{{ path('admin_dashboard') }}" class="btn btn-danger mr-2">Annuler</a>
    </div>
{{ form_end(form) }}
