<div class="mb-4">
    <div class="d-flex flex-column flex-md-row justify-content-between">
        <a href="{{ path('admin_dashboard') }}" class="btn btn-secondary mb-4"><u>Tableau de board</u></a>
        <a href="{{ path('admin_manage_pronostic') }}" class="btn btn-secondary mb-4"><u>Gérer mes pronostiques</u></a>
        <a href="{{ path('admin_manage_comment') }}" class="btn btn-secondary mb-4"><u>Gérer les commentaires</u></a>
        <a href="{{ path('admin_modify_home') }}" class="btn btn-secondary mb-4"><u>Modifier l'accueil</u></a>
        <a href="{{ path('pronostic_new') }}" class="btn btn-success mb-4">
            <i class="fas fa-plus-square"></i> Ajouter un pronostique
        </a>
    </div>
</div>