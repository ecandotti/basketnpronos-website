<div class="mb-4">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-center">
        <a href="{{ path('admin_dashboard') }}" class="btn btn-secondary mb-4">
            <i class="fas fa-tachometer-alt"></i>
        </a>
        <a href="{{ path('admin_manage_comment') }}" class="btn btn-secondary mb-4">
            <i class="fas fa-tasks"></i> Commentaires
        </a>
        <a href="{{ path('admin_manage_pronostic') }}" class="btn btn-secondary mb-4">
            <i class="fas fa-tasks"></i> Pronostique
        </a>
        <a href="{{ path('admin_manage_gallery') }}" class="btn btn-secondary mb-4">
            <i class="fas fa-tasks"></i> Galerie
        </a>
        <a href="{{ path('admin_modify_home') }}" class="btn btn-secondary mb-4">
            <u>Modifier l'accueil</u>
        </a>
        <a href="{{ path('pronostic_new') }}" class="btn btn-success mb-4">
            <i class="fas fa-plus-square"></i> Pronostique
        </a>
        <a href="{{ path('admin_add_gallery') }}" class="btn btn-info mb-4">
            <i class="fas fa-plus-square"></i> Galerie
        </a>
    </div>
</div>